The original site used Google Fonts. This production build removes the remote font dependency for faster loading. Add licensed local font files here if desired, then update style.css.
Place additional SVG/PNG icons here. The site favicon is already included.
